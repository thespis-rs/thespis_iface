package:

  name   : thespis
  version: 0.1.0
  authors: [ Naja Melan <najamelan@autistici.org> ]
  edition: '2018'

features:

  default: [ std, derive ]
  std       : [ futures-preview/std ]
  derive    : [ thespis_derive      ]

dependencies:

  futures-preview: { version: ^0.3.0-alpha.15, features: [ nightly ], default-features: false }
  thespis_derive : { path: ../thespis_derive, optional: true  }
  # failure        : ^0.1

