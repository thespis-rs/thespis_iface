package:

  name   : thespis
  version: 0.1.0
  authors: [ Naja Melan <najamelan@autistici.org> ]
  edition: '2018'

features:

  default: [ std ]
  std    : [ futures-preview/std ]
  derive : [ thespis_derive      ]

dependencies:

  futures-preview: { path: ../../futures-rs/futures, features: [ nightly ], default-features: false }
  thespis_derive : { path: ../thespis_derive, optional        : true  }
